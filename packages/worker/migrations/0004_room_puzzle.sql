-- ============================================================================
-- 0004：房间级的「AI 创作题目」
--
-- 题库平时是编译进 Worker 的常量（curated），但房主可以让 AI 现写一道题。
-- 这道题只属于这一个房间，所以直接存在 rooms 行里：
--   · puzzle_json 非空时，store.getPuzzle() 优先返回它（同 id 覆盖静态题库）
--   · 不污染全局题库、不需要额外查询（房间行本来每次请求都会读）
--   · 汤底与事实点只存在服务端这里，永远不经过 DTO 投影
-- 旧数据为 NULL，行为与之前完全一致。
-- ============================================================================
ALTER TABLE rooms ADD COLUMN puzzle_json TEXT;
