-- ============================================================================
-- 0003：开局前的「准备状态」
--
-- rooms.ready_json 存已举手「我准备好了」的成员 id 数组（JSON）。
--   · 只在 waiting / settled 阶段有意义，MATCH_BEGIN 时由规则层清空
--   · 成员离开或被房主踢出时会从数组里移除
--   · 离线与旁观成员不计入"资格"，不会把大家卡在开场前
-- 旧数据视为"无人准备"（默认 '[]'），与旧行为一致。
-- ============================================================================
ALTER TABLE rooms ADD COLUMN ready_json TEXT NOT NULL DEFAULT '[]';
