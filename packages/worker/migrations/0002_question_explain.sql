-- ============================================================================
-- 0002：questions 增加 explain 列
--
-- 「是 / 否」之后可以附一句简短的补充说明（由模型按需给出，≤30 字）。
-- 该字段只是"让结论更好理解"，因此：
--   · 没写 / 写得不合适 → 存 NULL，前端只显示裸的"是/否"
--   · 泄露检查不通过 → 服务端直接丢弃这一句，绝不影响这一轮判定的有效性
-- 旧数据不受影响（NULL 即"无补充说明"）。
-- ============================================================================
ALTER TABLE questions ADD COLUMN explain TEXT;
